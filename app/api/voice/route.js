import {createClient} from "../../../lib/supabase/server";
export async function POST(req){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return Response.json({error:'Nicht eingeloggt'},{status:401});const {text=''}=await req.json();if(!text.trim())return Response.json({error:'Text fehlt'},{status:400});if(!process.env.OPENAI_API_KEY)return Response.json({error:'OPENAI_API_KEY fehlt. Für Demo bitte den Voiceover-Schritt später aktivieren.'},{status:503});const r=await fetch('https://api.openai.com/v1/audio/speech',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_TTS_MODEL||'gpt-4o-mini-tts',voice:process.env.OPENAI_TTS_VOICE||'alloy',input:text,response_format:'mp3'})});if(!r.ok)return Response.json({error:`TTS API error: ${(await r.text()).slice(0,240)}`},{status:502});return new Response(await r.arrayBuffer(),{headers:{'Content-Type':'audio/mpeg','Content-Disposition':'attachment; filename="viralivo-voiceover.mp3"'}})}
