import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '../../lib/supabase/server';
export default async function Settings(){const sb=await createClient();const {data:{user}}=await sb.auth.getUser();if(!user)redirect('/login');const {data:p}=await sb.from('profiles').select('email,plan,credits').eq('id',user.id).single();return <main className="appShell"><header className="topbar"><Link className="brand" href="/">Viralivo <span>AI</span></Link><nav><Link href="/dashboard">Dashboard</Link> · <Link href="/billing">Billing</Link></nav></header><section className="settingsPage"><div className="pill">SETTINGS</div><h1>Dein Account</h1><div className="panel settingsCard"><div><span>E-Mail</span><strong>{p?.email||user.email}</strong></div><div><span>Plan</span><strong>{p?.plan||'free'}</strong></div><div><span>Verfügbare Videos</span><strong>{p?.credits??0}</strong></div><div><span>Sprache</span><strong>Deutsch / English</strong></div></div><Link className="button" href="/billing">Plan verwalten →</Link></section></main>}
